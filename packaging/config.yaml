# Default agent configuration (shipped with the aegis-agent package).
# Edit paths as needed; restart the service after changes: systemctl restart aegis

logging:
  path: /var/log/aegis/events.log
  format: json

rules:
  path: /etc/aegis/rules.yaml
