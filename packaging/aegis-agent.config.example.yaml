# Example configuration for aegis-agent (copy to /etc/aegis/config.yaml).
#
# Security events are written only to logging.path in the chosen format.

logging:
  path: /var/log/aegis/events.log
  format: json   # json | text

rules:
  path: /etc/aegis/rules.yaml
