#!/usr/bin/env bash
# Copy to step-2.3-toctou-lru.sh (without .example), chmod +x, and implement:
#   - TOCTOU race regression checks against the eBPF program
#   - LRU map orphan eviction behaviour
# This file is intentionally not executable so it is skipped by run-test.sh.

echo "step-2.3: template only — implement TOCTOU + LRU checks here"
exit 0
